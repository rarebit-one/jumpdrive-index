# Build output
/dist/
/jumpdrive-index
/starchart

# Local git worktrees (rarebit-one worktree-only workflow)
/.worktrees/

# Local env / secrets (never commit)
.env
*.local

# SQLite runtime artifacts
*.db
*.db-wal
*.db-shm

# Go
*.test
*.out
